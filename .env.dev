# Ambiente de Desenvolvimento

# Node.js
NODE_ENV=development

# MySQL (usado pelo container do db e pelo backend)
MYSQL_ROOT_PASSWORD=rootpassword
MYSQL_DATABASE=godmota_dev
MYSQL_USER=root
MYSQL_PASSWORD=rootpassword

# Backend API - variáveis para conexão à BD
DB_HOST=db
DB_PORT=3306
DB_USER=root
DB_PASSWORD=rootpassword
DB_NAME=godmota_dev

# Frontends (React)
REACT_APP_API_URL=http://localhost:5000  # ou http://backend-api:5000 se estiver em container

# Docker helpers (opcional, mas útil)
PUBLIC_FRONTEND_DOCKERFILE=Dockerfile.dev
ADMIN_FRONTEND_COMMAND=npm start
PUBLIC_FRONTEND_COMMAND=npm start
